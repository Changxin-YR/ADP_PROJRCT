# 桌面端指向后端服务器，例如 https://adp.example.com；留空时 Electron 默认使用 http://127.0.0.1:5001。
VITE_API_BASE_URL=
VITE_DEMO_MODE=false
# 演示范围：留空/未设置 = 完整档（九个业务分区全可见）；
# 设为 core = 核心档，只保留「塘口档案/塘口分组/养殖批次/出塘捕捞/销售明细/应收账款/成本构成/期间结算」主线。
# 用法：npm run dev:demo（等价于 vite --mode demo，读取本目录的 .env.demo）
VITE_DEMO_SCOPE=
