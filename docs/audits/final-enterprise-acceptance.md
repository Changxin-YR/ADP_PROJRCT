# ADP 最终企业业务验收

验收日期：2026-08-24（Asia/Shanghai）  
验收源码：`C:\Users\27363\Desktop\ADP` 与 `C:\Users\27363\Desktop\adp_phone`  
最终结果：PASS（微信预览上传需配置正式 AppID 并登录）

## 验收范围

- Web 管理端：账号权限、塘口批次、投喂作业、仓储、采购、销售、成本、数据交换、工作台与审计。
- Flask 后端：API、业务服务、数据访问、权限、审计、迁移与发布契约。
- Expo 移动 App：现场养殖、仓储、采购、销售、成本、数据交换、离线队列和移动会话。
- 微信小程序：29 个页面，覆盖工作台、投喂、日常作业、物料领退、塘口、批次、消息与个人中心。

## 生产基线与回滚证据

当前源码继承已验收生产基线 `20260817-732c247ecde1`，归档 SHA-256 为 `f629a90372b19b06790b175d5885e4b66a2905827982434214f8dd27f1484835`。数据库迁移编号为 `001` 至 `021`；当时后端基线为 `236 passed`。部署脚本继续保留备份校验、蓝绿切换、健康检查与受控回滚契约，本轮重点对合并后的最新源码和三客户端进行回归。

## 企业业务控制

七角色覆盖系统管理员、养殖管理员、养殖作业员、仓储管理员、采购人员、财务人员和销售人员，并验证岗位分离与越权拒绝。

| 控制项 | 验收标准 |
| --- | --- |
| 草稿删除 | 仅未提交且未被引用的草稿可删除 |
| 已提交数据 | 允许修订，版本递增并保留修订记录 |
| 核验后只读 | 前端只读，API 与数据库拒绝非法修改和删除 |
| 正式纠错 | 使用更正、冲销、退货或反结算，不覆盖原始台账 |
| 待办闭环 | 核验后进入完成历史并保留审计记录 |
| 岗位分离 | 经办人与核验人分离，越权 API 必须拒绝 |
| 财务凭据 | 付款、收款、交付、费用和资产核验前必须有凭据 |

## 自动化证据

| 门禁 | 2026-08-24 结果 |
| --- | --- |
| 后端 pytest（真实隔离 MySQL 9.7） | 397 passed，0 skipped，0 failed |
| 前端 Vitest | 17 个文件、102 passed |
| Vue 类型检查与生产构建 | 通过，185 个模块完成转换 |
| Playwright 完整 E2E | 32 passed，0 failed |
| Expo TypeScript 与 API 契约 | 通过，31 项当前 API 契约 |
| Expo Android 静态导出 | 通过，1472 个模块完成打包 |
| Expo Android 37 x86_64 实机门禁 | Expo Go 57.0.9 正常渲染登录页及创建账号页；热重载通过，错误日志 0 |
| 移动依赖安全门禁 | npm audit：critical 0、high 0；11 个 moderate 限于 Expo iOS 工程生成工具上游依赖 |
| 微信小程序源码门禁 | 29 个页面、33 个 JavaScript 文件、18 组当前 API 路由通过 |
| 微信开发者工具 | CLI 自动化载入与本地编译通过；服务器预览上传因 `touristappid` 被拒绝 |

本轮在 `127.0.0.1:3308` 启动全新可销毁 MySQL 9.7 隔离实例，补跑了默认安全配置会跳过的全部真实数据库集成门禁；测试结束后随机测试库均已删除，3308 已关闭。微信服务器预览上传要求把 `project.config.json` 的占位 AppID 替换为项目正式 AppID，并登录有权限的微信开发者账号；本地源码编译已通过。

## 已知排除

- 正式微信服务器预览与上传不使用游客 AppID；必须由项目正式 AppID 和授权账号完成。

## 最终结论

当前源码满足 Web、后端、移动 App 和微信小程序的统一业务与接口要求。Playwright 遗留桩文件已补齐并纳入完整 32 场景回归；移动 App 已完成 Expo SDK 57 兼容版本校准及 Android 模拟器真实渲染，移动 App 与小程序使用同一后端 API、权限和数据口径。正式发布仍需提供生产数据库、密钥、微信正式 AppID 和有权限的发布账号。
